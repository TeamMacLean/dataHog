function uploader(mountNode) {

  var fileUploader = React.createClass({

    render: function render() {
    }
  });

  ReactDOM.render(React.createElement(HelloMessage, {name: "John"}), mountNode);

  return fileUploader;
}

